# fastapi-react-mysql-docker
Full-Stack project with FastAPI, MySQL, and React  This repository includes a backend API built with FastAPI, a MySQL database, and a frontend built with React. All components are containerized using Docker and managed with Docker Compose for easy development and deployment.  Components: FastAPI backend, MySQL database, React frontend.
